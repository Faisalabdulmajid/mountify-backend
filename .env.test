# Test Environment Variables
NODE_ENV=test

# Database Configuration (Use test database)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=db_gunung2_test
DB_USER=postgres
DB_PASSWORD=test_password

# JWT Configuration
JWT_SECRET_KEY=test_jwt_secret_key_minimum_32_characters_for_security
JWT_EXPIRES_IN=1h

# CSRF Configuration
CSRF_SESSION_SECRET=test_csrf_session_secret_key_change_in_production_minimum_32_chars

# Upload Configuration
UPLOAD_MAX_FILE_SIZE=3145728
UPLOAD_PATH=./public/uploads
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/jpg,image/webp,image/gif

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001

# Dialogflow Configuration (Test)
GOOGLE_APPLICATION_CREDENTIALS=./config/credentials/dialogflow-service-account.json
DIALOGFLOW_PROJECT_ID=test-project
DIALOGFLOW_LANGUAGE_CODE=id
