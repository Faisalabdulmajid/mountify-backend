const { exec } = require("child_process");
const path = require("path");
const fs = require("fs");
const os = require("os");

// Middleware auth admin (pastikan sudah ada di project)
const auth = require("../middleware/auth");

module.exports = {};
